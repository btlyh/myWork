package com.cambrian.dfhm.skill;

import java.util.ArrayList;

import com.cambrian.dfhm.battle.BattleCard;

/**
 * 类说明：治疗技能
 * @author：Sebastian
 * 
 */
public class TreatSkill extends Skill{

	/* static fields */

	/* static methods */

	/* fields */
	/** 技能发动率 */
	private int rate;

	/* constructors */

	/* properties */

	/* init start */

	/* methods */
	@Override
	public ArrayList<Integer> skillValue(BattleCard attCard, int[] aim, BattleCard[] aimList, ArrayList<Integer> record) {
		int hurt = super.getSkillHurt(attCard);
		BattleCard aimCard;
		for (int i = 0; i < aim.length; i++) {
			aimCard = aimList[aim[i]];
			aimCard.incrHp(hurt);
			record.add(aimCard.getIndex());
			record.add(hurt);
			record.add(-1);
			System.err.println("治疗技能，照成作用对象==="+aimCard.getName()+"血量增加 ==="+hurt+", 剩余血量 ==="+aimCard.getCurHp());
		}
		return super.getDieIndex();
	}
	
	@Override
	public boolean buffValue(BattleCard aimCard) {
		return false;
	}
}
